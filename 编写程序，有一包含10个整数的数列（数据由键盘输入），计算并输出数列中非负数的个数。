#include<stdio.h>
int main()
{
	int arr[10], count=0;
	for (int i = 0; i < 10; i++)
	{
		scanf("%d", &arr[i]);
	}
	for (int i = 0; i < 10; i++)
	{
		if (arr[i] >= 0)count++;
	}
	printf("%d", count);
	return 0;
}
