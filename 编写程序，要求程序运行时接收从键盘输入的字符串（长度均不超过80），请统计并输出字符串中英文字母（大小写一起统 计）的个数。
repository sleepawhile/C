#include<stdio.h>
#include<string.h>
int main()
{
	char arr[80] = {'\0'};
	int count = 0;
	gets_s(arr);
	for (int i = 0; i < strlen(arr); i++)
	{
		if (arr[i] >= 'a' && arr[i] <= 'z' || arr[i] >= 'A' && arr[i] <= 'Z')count++;
	}
	printf("%d", count);
	return 0;
}
