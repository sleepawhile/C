#include<stdio.h>
int main()
{
	int arr[3][3], t;
	for (int i = 0; i < 3; i++)
	{
		for (int j = 0; j < 3; j++)
		{
			scanf("%d", &arr[i][j]);
		}
	}
	for (int i = 0; i < 3; i++)
	{
		int j = 0;
		while(j<3)
		{
			printf("%d ", arr[j][i]);
			j++;
		}
		if (j == 3)printf("\n");
	}
	return 0;
}
